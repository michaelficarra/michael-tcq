# Test environment — used by Playwright e2e tests.
# Runs on different ports from development to avoid conflicts.
# Ports are configured in playwright.config.ts
STORE=file
ADMIN_USERNAMES=admin
# DATA_DIR is set dynamically by playwright.config.ts to a fresh temp directory.
